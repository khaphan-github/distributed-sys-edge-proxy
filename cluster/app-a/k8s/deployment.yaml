# ConfigMap for Envoy Proxy Configuration
# Best Practice: Separate ConfigMap files for easier management and reusability
apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-config-app-a
  namespace: default
  labels:
    app: envoyrproxy
    component: config
  annotations:
    description: "Envoy proxy configuration files"
    version: "1.0"
data:
  # Envoy configuration file
  envoy.yaml: |
    admin:
      access_log_path: /tmp/admin_access.log
      address:
        socket_address:
          address: 0.0.0.0
          port_value: 9901

    static_resources:
      listeners:
        - name: app_a_sidecar_listener
          address:
            socket_address:
              address: 0.0.0.0
              port_value: 3200

          filter_chains:
            - filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: app_a_sidecar_hcm_filter
                    access_log:
                      - name: envoy.access_loggers.stdout
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog

                    http_filters:
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                    route_config:
                      name: app_a_sidecar_http_route_config
                      virtual_hosts:
                        - name: app_a_sidecar_virtual_host
                          domains:
                            - "*"
                          routes:
                            - name:
                              match:
                                prefix: "/"
                              route:
                                cluster: app_a_service

      clusters:
        - name: app_a_service
          type: STRICT_DNS
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: app_a_service
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: app-a-service
                          port_value: 9101
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-a-deployment
  labels:
    app: app-a

spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-a
  template:
    metadata:
      labels:
        app: app-a
    spec:
      containers:
        - name: app-a
          image: edge_proxy_app-a:v1.0.1
          ports:
            - containerPort: 9001
              name: flask-port
          imagePullPolicy: Never
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: SERVICE_B_URL
              value: "http://envoyrproxy-service-app-b:9220"
            - name: SERVICE_C_URL
              value: "http://envoyrproxy-service-app-c:9230"
---
apiVersion: v1
kind: Service
metadata:
  name: app-a-service
spec:
  type: ClusterIP
  selector:
    app: app-a
  ports:
    - name: app-a-svc-port
      protocol: TCP
      port: 9101
      targetPort: flask-port
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: envoyrproxy-app-a-deployment
  labels:
    app: envoyrproxy-app-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: envoyrproxy-app-a
  template:
    metadata:
      labels:
        app: envoyrproxy-app-a
    spec:
      volumes:
        - name: envoy-config-volume
          configMap:
            name: envoy-config-app-a
            # Mount specific file with proper permissions
            defaultMode: 0644
            items:
              - key: envoy.yaml
                path: envoy.yaml
      containers:
        - name: edge-proxy-envoy
          image: edge_proxy_envoy:latest
          ports:
            - containerPort: 3200
              name: envoy-port
          # Mount config files from ConfigMap
          volumeMounts:
            - name: envoy-config-volume
              mountPath: /etc/envoy
              readOnly: true
          imagePullPolicy: Never
          # Health checks (best practice)
          livenessProbe:
            httpGet:
              path: /stats
              port: 9901
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /stats
              port: 9901
            initialDelaySeconds: 5
            periodSeconds: 5
          # Resource limits (best practice)
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: envoyrproxy-service-app-a
spec:
  type: LoadBalancer
  selector:
    app: envoyrproxy-app-a
  ports:
    - name: envoyrproxy-svc-port
      protocol: TCP
      port: 9210
      targetPort: envoy-port
